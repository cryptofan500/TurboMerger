{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "TurboMerger default capabilities — dialog only; all file I/O is done by audited Rust commands, not the fs plugin",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
